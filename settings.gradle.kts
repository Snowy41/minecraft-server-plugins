rootProject.name = "minecraft-server"

include(
    "api-module",
    "core-plugin",
    "lobby-plugin",
    "partition-plugin"
    //"battleroyale-plugin",
    //"proxy-plugin"
)
include("partition")